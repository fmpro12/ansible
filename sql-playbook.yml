- name: Update web servers
  hosts: 127.0.0.1
  
  tasks:
  - name:  
    pip:
       name: "{{ item }}"
       state: present
    with_items:
          - PyMySQL
          - boto
          - boto3
          - botocore
  - name: lookup secretsmanager secret in the current region
    debug: msg="{{ lookup('amazon.aws.aws_secret', 'qa/anisble', region='us-west-2') }} "
    
  - name: Create database user with name 'bob2' and password '12345' with all database privileges
    community.mysql.mysql_user:
        login_host: 10.42.0.38
        login_user: admin
        login_password: zubur1
        name: bob2
        password: 12345
        priv: '*.*:ALL'
        state: present
